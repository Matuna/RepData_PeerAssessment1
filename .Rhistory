?mfrow
?par
?hist
?append
?append.row
??append.row
?rbind
setwd("E:/Coursera/Reproducible Research/Assignment1/RepData_PeerAssessment1")
activity<-read.csv("activity.csv")
activity$date<-as.Date(activity$date,format="%Y-%m-%d")
stepsday<-aggregate(steps~date,data=activity,sum)
Nas<-which(is.na(activity$steps))
Replace<-avgSteps$steps[which(avgSteps$interval==activity$interval[Nas])]
avgSteps<-aggregate(steps~interval,data=activity,mean)
Replace<-avgSteps$steps[which(avgSteps$interval==activity$interval[Nas])]
newActivity<-activity
NewActivity$steps[Nas]<-Replace
newActivity$steps[Nas]<-Replace
?weekday
?weekdays
?colbind
?cbind
cbind(newActivity,weekdays(newActivity$date))
View(newActivity)
?weekdays
a<-weekdays(newActivity$date)
newActivity<-cbind(newActivity,weekdays(newActivity$date))
View(newActivity)
Sys.setlocale("LC_TIME", "C")
names(newActivity)[4]<-"Weekday"
View(newActivity)
newActivity$Weekday<-weekdays(newActivity$date)
View(newActivity)
weekend<-which(newActivity$Weekday=="Saturday"|"Sunday")
weekend<-which(newActivity$Weekday=="Saturday"|newActivity$Weekday=="Sunday")
newAverageSteps<-aggregate(steps~date,data=newActivity,mean)
head activity
head (activity)
tail(activity)
View(newAverageSteps)
tail(newActivity)
head(newactivity)
head(newActivity)
tail(Nas)
tail(replace)
tail(Replace)
View(avgSteps)
View(newActivity)
View(activity)
read.csv("activity.csv")
activity<-read.csv("activity.csv")
activity$date<-as.Date(activity$date,format="%Y-%m-%d")
stepsDay<-aggregate(steps~date,data=activity,sum)
View(stepsDay)
avgSteps<-aggregate(steps~interval,data=activity,mean)
View(activity)
sum(is.na(activity$steps))
2304/288
Nas<-which(is.na(activity$steps))
tail(Nas)
Replace<-avgSteps$steps[which(avgsteps$interval==activity$interval[Nas])]
Replace<-avgSteps$steps[which(avgSteps$interval==activity$interval[Nas])]
tail(replace)
tail(Replace)
Replace<-data.frame(Replace)
View(Replace)
Nas<-data.frame(Nas)
View(Nas)
View(avgSteps)
?find
trial<-activity$interval[Nas]
Nas<-as.numeric(Nas)
Nas<-which(is.na(activity$steps))
trial<-activity$interval[Nas]
tail(trial)
View(avgSteps)
trial2<-which(trial==avgSteps$interval)
tail(trial2)
trial2<-which(avgSteps$interval==trial)
tail(trial2)
view trial2
view (trial2)
trial2<-data.frame(trial2)
View(trial2)
?where
trial2<-data.frame(which(trial %in% avgSteps$interval))
View(trial2)
trial2<-data.frame(which(avgSteps$interval %in% trial))
View(trial2)
activity<-read.csv("activity.csv")
activity$date<-as.Date(activity$date,format="%Y-%m-%d")
stepsDay<-aggregate(steps~date,data=activity,sum)
avgSteps<-aggregate(steps~interval,data=activity,mean)
Nas<-which(is.na(activity$steps))
Replace<-avgSteps$steps[which(avgSteps$Interval %in% activity$interval[Nas])]
sum(is.na(Replace))
newActivity<-activity
newActivity$steps[Nas]<-Replace
Replace<-avgSteps$steps[which(avgSteps$interval %in% activity$interval[Nas])]
newActivity$steps[Nas]<-Replace
sum(is.na(newActivity))
median(newActivity$steps)
mean(newActivity$steps)
newStepday<-aggregate(steps~date,data=newActivity,sum)
mean(newStepday$steps)
median(newStepday$steps)
?write.xls
write.table(activity, "justryin",sep="\t")
View(newStepday)
View(stepsDay)
View(newStepday)
mean(newStepday)
mean(newStepday$steps)
?mean
mean(newStepday$steps,na.rm=FALSE)
mean(newStepday$steps,na.rm=TRUE)
newavgSteps<-aggragate(steps~date,data=newActivity,mean)
newavgSteps<-aggregate(steps~date,data=newActivity,mean)
?weekday
?weekdays
weekday(newavgSteps$date)
weekdays(newavgSteps$date)
newavgSteps<-cbind(newavgSteps,weekdays(newavgSteps$date))
View(newavgSteps)
str(newavgSteos)
str(newavgSteps)
names(newavgSteps)[3]<-"Weekday"
View(newavgSteps)
weekday<-which(newavgSteps$Weekday=="Sunday"|newavgSteps$Weekday=="Saturday")
weekend<-which(newavgSteps$Weekday=="Sunday"|newavgSteps$Weekday=="Saturday")
weekday<-which(!newavgSteps$Weekday=="Sunday"|!newavgSteps$Weekday=="Saturday")
weekday<-which(!newavgSteps$Weekday=="Sunday"&!newavgSteps$Weekday=="Saturday")
newavgSteps<-cbind(newavgSteps,newavgSteps$Weekday)
View(newavgSteps)
names(newavgSteps)[4]<-weekend
names(newavgSteps)[4]<-"weekend"
View(newavgSteps)
newavgSteps$weekend[weekday]<-"weekday"
blisters<-seq(1,61)
blisters[weekdays]<-"weekdays"
blisters[weekday]<-"weekdays"
blisters[weekend]<-"weekend"
newavgSteps$weekend<-blisters
View(newavgSteps)
str(newavgSteps)
library(lattice)
?dim
dim(newActivity$steps)
dim(newActivity)
activity<-read.csv("activity.csv")
activity$date<-as.Date(activity$date,format="%Y-%m-%d")
stepsDay<-aggregate(steps~date,data=activity,sum)
a<-c(mean(stepsDay$steps,na.rm=TRUE),median(stepsDay$steps,na.rm=TRUE))
names(a)<-c("Mean","Median")
avgSteps<-aggregate(steps~interval,data=activity,mean)
Nas<-which(is.na(activity$steps))
Replace<-avgSteps$steps[which(avgSteps$interval %in% activity$interval[Nas])]
newActivity<-activity
newActivity$steps[Nas]<-Replace
newStepday<-aggregate(steps~date,data=newActivity,sum)
weekday<-which(!weekdays(newActivity$date)=="Sunday"&!weekdays(newActivity$date)=="Saturday")
weekend<-which(weekdays(newActivity$date)=="Sunday"|weekdays(newActivity$date)=="Saturday")
parse<-seq(1,dim(newActivity[1]))
parse[weekday]<-"weekday"
parse[weekend]<-"weekend"
newavgSteps<-cbind(newActivity,parse)
names(newActivity)[3]<-"Weekday"
weekday<-which(!weekdays(newActivity$date)=="Sunday"&!weekdays(newActivity$date)=="Saturday")
weekend<-which(weekdays(newActivity$date)=="Sunday"|weekdays(newActivity$date)=="Saturday")
parse<-seq(1,as.numeric(dim(newActivity[1]))
parse[weekday]<-"weekday"
parse[weekend]<-"weekend"
newavgSteps<-cbind(newActivity,parse)
names(newActivity)[3]<-"Weekday"
parse<-seq(1,as.numeric(dim(newActivity[1]))
weekday<-which(!weekdays(newActivity$date)=="Sunday"&!weekdays(newActivity$date)=="Saturday")
weekend<-which(weekdays(newActivity$date)=="Sunday"|weekdays(newActivity$date)=="Saturday")
parse<-seq(1,as.numeric(dim(newActivity[1])))
parse[weekday]<-"weekday"
parse[weekend]<-"weekend"
newavgSteps<-cbind(newActivity,parse)
names(newActivity)[3]<-"Weekday"
dim(newActivity)
dim(newActivity)[1]
as.numeric(dim(newActivity)[1])
seq(1,as.numeric(dim(newActivity)[1]))
weekday<-which(!weekdays(newActivity$date)=="Sunday"&!weekdays(newActivity$date)=="Saturday")
weekend<-which(weekdays(newActivity$date)=="Sunday"|weekdays(newActivity$date)=="Saturday")
parse<-seq(1,as.numeric(dim(newActivity)[1]))
parse[weekday]<-"weekday"
parse[weekend]<-"weekend"
newavgSteps<-cbind(newActivity,parse)
names(newActivity)[3]<-"Weekday"
View(newActivity)
activity<-read.csv("activity.csv")
activity$date<-as.Date(activity$date,format="%Y-%m-%d")
stepsDay<-aggregate(steps~date,data=activity,sum)
avgSteps<-aggregate(steps~interval,data=activity,mean)
Nas<-which(is.na(activity$steps))
Replace<-avgSteps$steps[which(avgSteps$interval %in% activity$interval[Nas])]
newActivity<-activity
newActivity$steps[Nas]<-Replace
newStepday<-aggregate(steps~date,data=newActivity,sum)
weekday<-which(!weekdays(newActivity$date)=="Sunday"&!weekdays(newActivity$date)=="Saturday")
weekend<-which(weekdays(newActivity$date)=="Sunday"|weekdays(newActivity$date)=="Saturday")
parse<-seq(1,as.numeric(dim(newActivity)[1]))
parse[weekday]<-"weekday"
parse[weekend]<-"weekend"
newActivity<-cbind(newActivity,parse)
names(newActivity)[3]<-"Weekday"
View(newActivity)
names(newActivity)[3]<-"interval"
names(newActivity)[4]<-"weekday"
newActiInter<-aggregate(steps~interval+weekend,data=newActivity,sum)
newActiInter<-aggregate(steps~weekend+interval,data=newActivity,sum)
newActiInter<-aggregate(steps~weekend|interval,data=newActivity,sum)
View(newActiInter)
?aggregate
newActiInter<-aggregate(steps~list(weekend,interval),data=newActivity,sum)
newActiInter<-aggregate(steps,list(weekend=weekend,interval=interval),data=newActivity,sum)
newActiInter<-aggregate(newActivity.steps,list(weekend=weekend,interval=interval),data=newActivity,sum)
newActiInter<-aggregate(steps,list("weekend","interval"),data=newActivity,sum)
View(newActiInter)
View(newActivity)
newActiInter<-aggregate(newActivity$steps,list("weekend","interval"),data=newActivity,sum)
newActiInter<-aggregate(steps~c(weekend,interval),data=newActivity,sum)
newActiInter<-aggregate(newActivity,list(weekend=weekend,interval=interval),mean)
newActiInter<-aggregate(newActivity,list(weekend=weekend),mean)
newActiInter<-aggregate(steps~weekend+interval=="weekday",data=newActivity,sum)
newActiInter<-aggregate(steps~interval+weekend=="weekday",data=newActivity,sum)
newActiInter<-aggregate(newActivity,by=list(newactivity$interval,newActivity$weekday),sum)
newActiInter<-aggregate(newActivity,by=list(newActivity$interval,newActivity$weekday),sum)
newActiInter<-aggregate(newActivity$steps,by=list(newActivity$interval,newActivity$weekday),sum)
View(newActiInter)
newActiInter<-aggregate(steps,by=list(interval,weekday),data=newActivity,sum)
newActiInter<-aggregate(newActivity$steps,by=list(interval,weekday),data=newActivity,sum)
newActiInter<-aggregate(newActivity$steps,by=list(newActivity$interval,weekday),data=newActivity,sum)
newActiInter<-aggregate(newActivity$steps,by=list(newActivity$interval,newActivity$weekday),data=newActivity,sum)
newActiInter<-aggregate(newActivity$steps,by=list(newActivity$interval,newActivity$weekday),sum)
newActiInter<-aggregate(newActivity$steps,by=list(interval=newActivity$interval,weekday=newActivity$weekday),sum)
View(newActiInter)
View(newActivity)
activity<-read.csv("activity.csv")
activity$date<-as.Date(activity$date,format="%Y-%m-%d")
stepsDay<-aggregate(steps~date,data=activity,sum)
a<-c(mean(stepsDay$steps,na.rm=TRUE),median(stepsDay$steps,na.rm=TRUE))
names(a)<-c("Mean","Median")
avgSteps<-aggregate(steps~interval,data=activity,mean)
Nas<-which(is.na(activity$steps))
Replace<-avgSteps$steps[which(avgSteps$interval %in% activity$interval[Nas])]
newActivity<-activity
newActivity$steps[Nas]<-Replace
newStepday<-aggregate(steps~date,data=newActivity,sum)
b<-c(mean(newStepday$steps),median(newStepday$steps))
names(b)<-c("Mean","Median")
weekday<-which(!weekdays(newActivity$date)=="Sunday"&!weekdays(newActivity$date)=="Saturday")
weekend<-which(weekdays(newActivity$date)=="Sunday"|weekdays(newActivity$date)=="Saturday")
parse<-seq(1,as.numeric(dim(newActivity)[1]))
parse[weekday]<-"weekday"
parse[weekend]<-"weekend"
newActivity<-cbind(newActivity,parse)
names(newActivity)[4]<-"Weekday"}
weekday<-which(!weekdays(newActivity$date)=="Sunday"&!weekdays(newActivity$date)=="Saturday")
weekend<-which(weekdays(newActivity$date)=="Sunday"|weekdays(newActivity$date)=="Saturday")
parse<-seq(1,as.numeric(dim(newActivity)[1]))
parse[weekday]<-"weekday"
parse[weekend]<-"weekend"
newActivity<-cbind(newActivity,parse)
names(newActivity)[4]<-"Weekday"
newActiInter<-aggregate(newActivity$steps,by=list(interval=newActivity$interval,weekday=newActivity$weekday),sum)
View(newActivity)
newActiInter<-aggregate(newActivity$steps,by=list(interval=newActivity$interval,weekday=newActivity$Weekday),sum)
View(newActiInter)
xyplot(steps~interval|weekday,newActiInter)
names(newActiInter)[1]<-"steps"
xyplot(steps~interval|weekday,newActiInter)
str(newActiInter)
View(newActiInter)
newActiInter<-aggregate(newActivity$steps,by=list(interval=newActivity$interval,weekday=newActivity$Weekday),sum)
names(newActiInter)[3]<-"steps"
View(newActiInter)
xyplot(steps~interval|weekday,newActiInter)
xyplot(steps~interval|weekday,newActiInter,type="l")
?lattice
?xyplot
